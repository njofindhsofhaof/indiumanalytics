{
  "framework": "nextjs",
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "crons": [
    {
      "path": "/api/revalidate?secret=${REVALIDATE_SECRET}",
      "schedule": "0 6 * * 1"
    }
  ]
}
